ADDITIVE/EDIT ONLY — do NOT remove any content, sections, text, or functionality. This is a pure visual UI upgrade + one section reorder.

READ src/app/page.tsx first, then make ALL of these changes:

== SECTION REORDER ==
Move the Telegram Growth Engine section (the one with '1,284 leads scraped', 47 Groups, 1,284 Leads, 612 DMs, £142k Pipeline stats card) to render IMMEDIATELY after the hero section (after the terminal/code block). Keep all its content 100% intact, just move its position up.

== GLOBAL DARK THEME FIX ==
Any section currently using a white or light background (bg-white, bg-gray-50, bg-slate-50, or similar) must be changed to bg-[#0a0a0f] or bg-[#0d0d14]. Text that was dark on white must become white/light on dark. Do NOT change any content.

== HERO SECTION ==
Add a radial mesh gradient behind the hero text: a large blurred circle `position:absolute, inset-0, z-index:0` using `bg-[radial-gradient(ellipse_80%_50%_at_50%_-20%,rgba(120,80,255,0.25),transparent)]`. Wrap existing hero content in a relative z-10 div so it stays on top.
Make the H1 use a gradient text: `bg-gradient-to-r from-white via-purple-200 to-blue-300 bg-clip-text text-transparent`
The 'v2.0.0 Now Live' badge: give it `bg-purple-900/40 border border-purple-500/30 text-purple-300` pill styling.
'Start Growth Engine' primary button: `bg-gradient-to-r from-violet-600 to-blue-500 hover:from-violet-500 hover:to-blue-400 text-white rounded-xl px-8 py-3 font-semibold shadow-lg shadow-violet-500/25 transition-all`
'View System Architecture' secondary button: `bg-white/5 border border-white/10 hover:bg-white/10 text-white rounded-xl px-8 py-3 font-semibold transition-all`

== STATS BAR (500+ clients, $2M+, 50+, 98%) ==
Give each stat its own accent color: first stat color text-violet-400, second text-cyan-400, third text-emerald-400, fourth text-pink-400.
Section background: `bg-gradient-to-r from-[#0d0d18] via-[#0f0f1f] to-[#0d0d18]` with a subtle top border `border-t border-white/5`.

== TRUST BAR (company logos) ==
Background: keep dark. Make company names use `text-white/30 hover:text-white/60 transition-colors` and add `tracking-widest text-xs uppercase`.

== 'AGENCY MODEL IS BROKEN' SECTION ==
Change background to `bg-[#07070f]`. All headings to `text-white`. Body text to `text-zinc-400`. The ❌ icons: change to a dark circle with red glow `text-red-500 bg-red-500/10 rounded-full p-1`. Section title: apply gradient text `from-white to-zinc-400`.
Add a left purple accent border on each pain point item: `border-l-2 border-purple-500/40 pl-4`.

== WHY WEMAGNIFAI CARDS ==
Upgrade each card to glassmorphism: `bg-white/[0.03] backdrop-blur-sm border border-white/[0.08] hover:border-purple-500/30 hover:bg-white/[0.05] transition-all duration-300 rounded-2xl`.
Each icon container: `bg-gradient-to-br from-violet-600/20 to-blue-500/20 rounded-xl p-3 w-fit`.

== SYSTEM ARCHITECTURE CARDS ==
Same glassmorphism treatment as Why WeMagnifAI cards above.
Section background: add `bg-[radial-gradient(ellipse_60%_40%_at_50%_50%,rgba(88,28,255,0.08),transparent)]`.

== 'GET THE AI EDGE WEEKLY' NEWSLETTER SECTION ==
Change from bright green to: `bg-gradient-to-br from-violet-900/60 to-blue-900/40 border border-violet-500/20 rounded-3xl`.
Email input: `bg-white/5 border border-white/10 text-white placeholder:text-white/30 rounded-xl`.
Button: `bg-gradient-to-r from-violet-600 to-blue-500 text-white rounded-xl font-semibold`.

== 'STEAL OUR BRAIN' SECTION ==
Change the abrupt purple/blue background break to a smooth `bg-gradient-to-b from-[#0a0a14] to-[#0d0d1e]` so it flows with the page.

== BOOK YOUR GROWTH STRATEGY AUDIT ==
Upgrade the card border to: `border border-violet-500/30` with `bg-gradient-to-br from-[#12101f] to-[#0d0b1a]`.
The top accent bar (currently blue-purple gradient line): make it `h-[2px] bg-gradient-to-r from-violet-500 via-pink-500 to-blue-500`.
Button: `bg-white text-black hover:bg-zinc-100 rounded-xl font-bold`.

== FOOTER ==
Background: `bg-[#05050a]` with a top `border-t border-white/5`.
Footer link text: `text-zinc-500 hover:text-white transition-colors`.

After making ALL changes: restart the app, take a screenshot, confirm 200 OK and no white sections remain visible. Do not change any text content anywhere.